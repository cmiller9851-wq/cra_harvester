services:
  - type: web
    name: cra-harvester-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: NODE_AUTH_TOKEN
        sync: false # Set this value manually in the Render UI
      - key: PYTHON_VERSION
        value: 3.10.0
